<h1 class="mb-3">New Payment</h1>

<%= render partial: 'form', locals: { payment: @payment, customer: @customer } %>

<%= link_to 'Back to Customer', customer_path(@customer), class: 'btn btn-secondary mt-3' %>

